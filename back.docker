FROM python:3.7-slim

ENV PYTHONUBUFFERED=1
RUN mkdir /app ~/.metta

COPY back/Pipfile /.metta/Pipfile
RUN python -m pip install --upgrade pip && \
    python -m pip install pipenv
RUN cd /.metta/ && pipenv install
RUN cat /.metta/Pipfile

WORKDIR /app
